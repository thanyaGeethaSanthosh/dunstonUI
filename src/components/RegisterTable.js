import React from 'react';

const RegisterTable = (props) => {
  // const rows = props.table.map((regRow, index) => (
  //   <div key={index}>{JSON.stringify(regRow)}</div>
  // ));
  return <div>{props.table}</div>;
};

export default RegisterTable;
